---
- name: Restart courier
  ansible.builtin.systemd:
    name: clawmaster-courier
    state: restarted
    daemon_reload: true

- name: Reload Caddy
  ansible.builtin.systemd:
    name: caddy
    state: reloaded

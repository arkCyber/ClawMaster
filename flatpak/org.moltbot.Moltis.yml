app-id: org.moltbot.Moltis
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: clawmaster

finish-args:
  - --share=network

build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
  env:
    CARGO_HOME: /run/build/clawmaster/cargo

modules:
  - name: clawmaster
    buildsystem: simple
    build-commands:
      - cargo build --release --manifest-path=crates/cli/Cargo.toml
      - install -Dm755 target/release/clawmaster /app/bin/clawmaster
    sources:
      - type: dir
        path: ..

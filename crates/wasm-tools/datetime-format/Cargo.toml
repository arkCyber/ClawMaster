[package]
name = "datetime-format"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.34"
time = { version = "0.3", features = ["formatting", "parsing"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
